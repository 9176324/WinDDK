#############################################################################
#
#       Copyright (C) Microsoft Corporation 1998
#       All Rights Reserved.
#                                                                          
#       MAKEFILE for gfx filter sample
#
#############################################################################

#
# DO NOT EDIT THIS FILE!!!  Edit .\sources. if you want to add a new source
# file to this component.  This file merely indirects to the real make file
# that is shared by all the components of NT.
#
# The WDK has also build environments for WinXP, Win2k and WinME. This sample
# however, only compiles well with the XP build environment, Therefore we
# need to prevent compilation on the other build environments.
!if defined(DDK_TARGET_OS) && "$(DDK_TARGET_OS)"=="WinXP" && !defined(AMD64) && !defined(IA64)
!INCLUDE $(NTMAKEENV)\makefile.def
!else
!message This project is for 32-bit builds of Windows XP only.
!endif
